network:
  version: 2
  renderer: networkd
  ethernets:
{% for interface in interfaces %}
    {{ interface.name }}:
      match:
          macaddress: "{{ interface.mac }}"
      set-name: {{ interface.name }}
      dhcp4: true
{% endfor %}
